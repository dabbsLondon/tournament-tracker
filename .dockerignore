# =============================================================================
# .dockerignore — Keeps Docker build contexts small and secrets out of images
# =============================================================================

# Rust build artifacts (14 GB+ — never send to Docker daemon)
target/

# Node / JS tooling (only needed for JS unit tests, not runtime)
node_modules/
coverage/

# Local data & logs (mount as volumes at runtime instead)
data/
logs/

# Secrets — NEVER bake credentials into images
.env

# Git internals
.git/
.gitignore

# IDE / editor files
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Claude Code internals
.claude/

# Backup files generated by sync process
*.bak

# CI/test artefacts
*.lcov
lcov.info
