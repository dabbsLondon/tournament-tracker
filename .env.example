# =============================================================================
# .env.example — Copy this to .env and fill in your values
# .env is git-ignored and never baked into Docker images
# =============================================================================

# Anthropic API key (required for AI-powered extraction)
ANTHROPIC_API_KEY=sk-ant-...

# Best Coast Pairings credentials (optional — for BCP data ingestion)
BCP_EMAIL=
BCP_PASSWORD=
BCP_AUTH_TOKEN=
BCP_REFRESH_TOKEN=
